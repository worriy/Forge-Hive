
@model IEnumerable<Hive.Backend.DataModels.UserProfile>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>UserProfile Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
				<th>
            @Html.DisplayNameFor(m => m.Id)
        </th>
				<th>
            @Html.DisplayNameFor(m => m.Id)
        </th>
				<th>
            @Html.DisplayNameFor(m => m.Job)
        </th>
				<th>
            @Html.DisplayNameFor(m => m.Department)
        </th>
				<th>
            @Html.DisplayNameFor(m => m.City)
        </th>
				<th>
            @Html.DisplayNameFor(m => m.Country)
        </th>
				<th>
            @Html.DisplayNameFor(m => m.PictureId)
        </th>
		        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
				<th>
            @Html.DisplayFor(modelItem => item.Id)
        </th>
				<th>
            @Html.DisplayFor(modelItem => item.Id)
        </th>
				<th>
            @Html.DisplayFor(modelItem => item.Job)
        </th>
				<th>
            @Html.DisplayFor(modelItem => item.Department)
        </th>
				<th>
            @Html.DisplayFor(modelItem => item.City)
        </th>
				<th>
            @Html.DisplayFor(modelItem => item.Country)
        </th>
				<th>
            @Html.DisplayFor(modelItem => item.PictureId)
        </th>
		        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
